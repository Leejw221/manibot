"""학습 손실 — 정책 밖에 둔다.

정책은 "관측 -> 행동" 만 알고, 선호 최적화처럼 **정책 두 개**(pi_theta, pi_ref)가 필요한
손실은 여기서 조립한다. 그래야 체크포인트가 평범한 정책 그대로 저장돼 eval·배포 경로가
안 깨진다 (wrapper 로 감싸면 state_dict 키가 어긋나는데 load 가 strict=False 라 **조용히
아무것도 안 실린다**).
"""
